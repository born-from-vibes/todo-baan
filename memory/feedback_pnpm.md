---
name: Use pnpm
description: User prefers pnpm over npm for package management
type: feedback
---

Always use `pnpm` instead of `npm` for installing packages and running scripts.

**Why:** User preference — they use pnpm as their package manager.

**How to apply:** Use `pnpm install`, `pnpm run dev`, etc. Never use `npm install` or `npm run`.
